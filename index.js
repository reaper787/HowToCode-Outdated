var shaarav9 = mouseX,xy, mouseY;
 fill(204, 255, 0);
 var sunSize = 50;
 ellipse(20, 20, sunSize, sunSize);
var house = [getImage("cute/RoofEast"),
 getImage("cute/RoofWest")];
 var i = 0;
var sunSize = 50;
var cre = getImage("avatars/duskpin-ultimate");
    

ellipse(shaarav9, shaarav9, 10, 10);
if(mouseIsPressed){draw= function() {
var sunSize = 50;
    sunSize+=2;
    var sunSize = 50;
    fill(mouseX, mouseY, mouseY);
    ellipse(mouseX, mouseY, mouseX, mouseY);
    ellipse(20, 20, sunSize, sunSize);
};} 
